var should = require('chai').should(),
    npmarmory = require('../index'),
    cat = npmarmory.cat;

describe('#cat', function(){
    it('should return some text', function() {
        var foobar = {
            cat.should.equal('meow');
        }
    })
}